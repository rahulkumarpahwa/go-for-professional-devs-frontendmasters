L17
How do you create a PickUpItem method for a player in Go?
Use the append function to add the item to the player's inventory slice, such as p.Inventory = append(p.Inventory, item), and optionally print a confirmation message like 'Picked up an item'

What is the strategy for implementing a DropItem method in Go?
Iterate through the inventory, find the item by name, then create a new slice by appending the slice before the item and the slice after the item using slice concatenation with the ellipsis operator

How can you create an item struct in Go for a role-playing game?
Define a struct with fields like Name and Type, for example:
type Item struct {
    Name string
    Type string
}

What approach can be used to implement a UseItem method for a player?
Range through the player's inventory, find the specific item by name, perform an action (like healing), and then remove the item from the inventory using slice manipulation

When appending items to a slice in Go, how does memory allocation work?
Go reuses the underlying array's length and capacity when appending, replacing values without necessarily creating an entirely new data structure, which helps maintain memory efficiency

L18
What is the recommended approach for structuring the main.go file in a Go project?
Keep main.go slim and simple, using it as an entry point to the application. Use other data structures and packages to handle core implementation and functionality.

When designing a package in Go, what is the first question to ask about data handling?
Determine if a struct is needed. Ask: 'Does this package need a struct?' A struct is a good solution when you need to pass resources around the application.

What are the two components returned by a constructor function in Go?
A constructor typically returns a pointer to a struct (e.g., a pointer to Application) and a potential error.

What is the purpose of using a logger in a Go application?
A logger provides a structured way to output messages, diagnose errors, and perform print debugging across the application. It can include additional information like timestamps.

In Go, when should the PANIC keyword be used?
PANIC should be reserved for situations that should never occur and where the application should completely stop if an error happens. It is not recommended for controlled or expected errors.

L19
What method is used to start an HTTP server in Go?
Use server.ListenAndServe() method, which starts the server and begins listening for incoming HTTP requests

What are the key timeout configurations for an HTTP server in Go?
IdleTimeout (maximum time to wait for next request when Keepalives are enabled), ReadTimeout (maximum duration for reading entire request), and WriteTimeout (maximum time for writing response)

What are the two arguments required for an HTTP handler function in Go?
An HTTP ResponseWriter (w) and an HTTP Request pointer (r), with signatures matching http.ResponseWriter and *http.Request

How do you bind a function to a specific route in a Go HTTP server?
Use http.HandleFunc(), which takes a path (like "/health") and the handler function as arguments

Why is the HTTP request parameter (r) a pointer in Go HTTP handlers?
To efficiently persist and modify client request data, especially when working with middleware or request body processing

L20
What Go standard library package allows parsing command-line flags?
The flag package enables parsing command-line flags when executing a Go program, allowing users to specify parameters like port numbers with default values.

How do you define a default flag value in Go's flag package?
When using flag.IntVar(), you can specify a default value as the third parameter, such as setting a default port to 8080 if no flag is provided.

What is the purpose of flag.Parse() in Go?
flag.Parse() is essential for processing the command-line flags and populating the variables defined with flags, enabling the application to use the passed values.

What is the difference between fmt.Printf() and fmt.Sprintf() in Go?
fmt.Printf() prints directly to standard output, while fmt.Sprintf() returns a formatted string that can be stored or manipulated further.

How can environment variables be retrieved in Go?
Go provides os.Getenv() to retrieve environment variable values by their key, and third-party packages can make environment variable management even easier.

L21
What is the primary purpose of the Chi package in Go?
Chi is a lightweight, idiomatic, and composable router for building Go HTTP services that implements standard library interfaces and allows for easy routing and middleware handling

What does the go.sum file represent in a Go project?
The go.sum file provides exact hashes for dependencies pulled into the application, similar to package-lock.json in web development, to ensure consistent and reproducible builds

What method is used to create a new router in Chi?
The chi.NewRouter() method is used to create a new router in the Chi package

How do you define a GET route using Chi?
Use the r.Get() method, specifying the path and handler function, such as r.Get('/health', app.HealthCheck)

What is the primary benefit of using method receivers on structs in Go?
Method receivers allow you to define methods that are associated with specific struct types, enabling object-oriented-like behavior and more organized code structure

L22
// curl localhost:8080/workouts/2
// curl -X POST localhost:8080/workouts

What method is used to extract URL parameters using the chi router?
chi.URLParam() is used to extract URL parameters from the HTTP request

What standard library package is used for string conversion to integers?
strconv package is used for string conversion, specifically the ParseInt() function

How is the application structure organized to manage handlers and routes?
An application struct is used to contain handlers, which can then be passed to routes to centralize access to different handler methods

What are the two HTTP methods implemented for the workout routes?
GET method for retrieving a workout by ID (/workouts/{id}) and POST method for creating a workout (/workouts)

What is the purpose of creating separate packages like 'api' and 'store'?
To organize code by separating concerns, such as API handlers and database interactions, making the application more modular and maintainable

L23
What command is used to ignore database files in Git?
Use touch .gitignore to create a gitignore file, then add /database and database to the file to prevent database files from being pushed to GitHub.

What is the purpose of defining a volume in a Docker Compose configuration?
To persist data between Docker container restarts, preventing data loss when the database is stopped and started

What is the default port for Postgres?
5432

What Docker Compose command is used to start a containerized database?
docker compose up --build (must go to the folder which has yml file)

L24
Which Postgres SQL driver is recommended for Go, and how can it be installed?
PGX is recommended, and it can be installed using 'go get github.com/jackC/pgx/v4/stdlib'

What are the typical connection parameters for connecting to a local Postgres database in this example?
host=localhost, user=postgres, password=postgres, dbname=postgres, port=5432, sslmode=disable

What Go standard library package is used for database connections?
database/sql package

How does Go's defer function work in the context of database connection closing?
Defer ensures a function is called at the end of the program's execution, after all other operations are complete, which is useful for closing database connections

What method is used to open a database connection in Go?
sql.Open() method, which takes a driver name and a connection string as arguments

L25
What is Goose and what primary purpose does it serve in database management?
Goose is a database migration tool for Go that allows modifications to database schema structure, enabling developers to add, change, or revert database changes safely and flexibly

What are the key fields in the users table migration?
The users table includes ID (BIGSERIAL primary key), username (VARCHAR, unique, not null), email (VARCHAR, unique, not null), password_hash (VARCHAR), bio (text), created_at (timestamp), and updated_at (timestamp)

What is the relationship between workouts and workout_entries in the database schema?
Workouts and workout_entries have a one-to-many relationship, where one workout can have multiple workout entries. The workout_entries table has a foreign key referencing the workouts table with ON DELETE CASCADE

What constraint is added to the workout_entries table to validate entries?
A constraint is added to ensure that a workout entry must have either reps or duration_seconds non-null, but not both, allowing for different types of exercises like weightlifting or running

What fields are included in the workout_entries table?
The workout_entries table includes ID, workout_ID (foreign key), exercise_name, sets, reps, duration_seconds, weight, notes, order_index, created_at, and updated_at

L26

What two arguments does the migrate function take?
The migrate function takes a pointer to sql.DB and a directory string as arguments

How does Goose determine which migration files to run?
Goose uses goose.Up() to run migration files from a specified directory, and goose.SetBaseFS() to set the file system containing the migration files

What command can be used to connect to the Postgres database and verify migrations?
psql -h localhost -p 5432 can be used to connect to the Postgres database, and \dt command lists the database tables

What does the MigrateFS() function do in the database migration process?
MigrateFS() sets the base file system for migrations, calls the Migrate function, and ensures the base file system is reset after migration


L27
What is the purpose of a struct tag in Go, specifically the json tag?
A struct tag helps with encoding and decoding JSON into a struct, allowing easy conversion between JSON payloads and struct fields with matching tag names.

Why might a field like 'Reps' be defined as a pointer to an int in a struct?
To explicitly allow checking for nil values, which provides more flexibility in handling optional or potentially unset numeric fields.

What is an interface in Go?
An interface is a collection of method signatures that defines a set of behaviors. When a type implements all the methods in an interface, it can be used as that interface type.

How does using an interface help with database decoupling in Go?
By defining a WorkoutStore interface with method signatures, the application layer can work with the interface without being tied to a specific database implementation, making future migrations easier.

What are the two methods defined in the WorkoutStore interface?
CreateWorkout (which takes a pointer to a Workout and returns a pointer to a Workout and an error) and GetWorkoutByID (which takes an int64 ID and returns a Workout or an error).

L28
What method is used to decode JSON request body in Go?
json.NewDecoder(r.Body).Decode(&workout) is used to parse JSON from the request body into a struct

What is the purpose of using an interface like WorkoutStore in this context?
The interface allows decoupling the API layer from the specific database implementation, enabling easier database swapping and maintaining flexibility across application layers

How do you set the response content type in an HTTP handler?
Use w.Header().Set("Content-Type", "application/json") to set the response content type to JSON

How can you encode a response struct back to the client?
Use json.NewEncoder(w).Encode(createdWorkout) to write the struct as a JSON response

What status code represents an internal server error in HTTP?
http.StatusInternalServerError (500) represents an internal server error when something goes wrong processing a request

L29
Note : 
We can't rollback after commit.

What is the purpose of using a database transaction in the CreateWorkout method?
To ensure that both the workout and its associated workout entries are committed together, maintaining data integrity. If any part of the process fails, the transaction can be rolled back to prevent partial data insertion.

What PostgreSQL notation is used for parameterized queries in this code?
The money symbol ($1, $2, $3, $4) is used to represent query parameters, with the number corresponding to the order of values being inserted.

What fields are inserted when creating a workout entry?
workout_id, exercise_name, sets, reps, duration_seconds, weight, notes, and order_index

How is database transaction error handling implemented in the CreateWorkout method?
A deferred transaction rollback is used, and errors are checked at each stage of the process. If an error occurs, the method returns nil and the error, and the transaction can be rolled back to its previous state.

L30
What command can be used to view all schemas created through Goose migration in PostgreSQL?
\dt command in PSQL can be used to view all schemas created through Goose migration

How can you verify database entries after creating a new workout?
Use 'select all from workouts' and 'select all from workout_entries' SQL commands to view the newly inserted data

What key attributes are included when creating a new workout entry?
Title, description, duration minutes, calories burned, and a list of workout entries with exercise names

What columns are displayed when selecting from the workouts table?
ID, title, description, duration, calories burned, created at, and updated at

How can you confirm that workout entries are correctly linked to a workout?
Check that the workout_entries table has a workout_id column matching the ID of the corresponding workout in the workouts table

DOCKER COMMAND TO CONNECT TO DB :
docker exec -it workoutDB psql -U postgres -h localhost -p 5432

L31
What SQL method is used to execute 
a query that returns at most one row?
QueryRow is used to execute a query that is expected to return at most one row

What error type indicates that no rows were returned from a database query?
sql.ErrNoRows is returned by Row scan when a database query does not return any rows

When retrieving database rows, what method is used to iterate through multiple results?
rows.Next() is used to iterate through multiple database query results

What function is used to add items to a slice in Go?
append() is used to add items to a slice

In a database query, what keyword is used to specify the order of returned results?
ORDER BY is used to specify the order of returned database results

L32

What method is used to handle updating workout entries in this implementation?
A full patch update where all existing workout entries are deleted and then reinserted with the new data, instead of updating individual entries

How does the code check if an update actually modified any rows?
By using the RowsAffected() method on the query result, which returns the number of rows affected, and checking if rowsAffected is zero

What database operation is used to manage the update process to ensure data consistency?
A database transaction is used, which allows beginning a transaction, performing updates, and then either committing the changes or rolling back if an error occurs

In the SQL update query for workouts, what fields are being updated?
Title, description, duration_minutes, and calories_burned, using a query that sets these fields where the workout ID matches

What is the approach for inserting updated workout entries after deletion?
Iterating through the workout entries, constructing an INSERT query with fields like workout_id, exercise_name, sets, reps, duration_seconds, weight, notes, and order_index

L33

What is the purpose of creating a pointer-based struct for update workout requests?
To allow checking if values are truly nil or intended to be empty, which helps distinguish between intentional updates and zero values. This enables more precise server-side validation by differentiating between unset fields and explicitly empty fields.

Why are pointer types used for Title, Description, DurationMinutes, and CaloriesBurned in the update request struct?


Because pointer types have a nil zero value, which allows distinguishing between unset fields and fields with zero/empty values. This provides more confidence in processing client requests by enabling precise field update checks.

What is the key difference between using pointer and non-pointer types for struct fields during JSON decoding?
Pointer types allow checking if a field was explicitly set or not, while non-pointer types default to zero values. This enables more granular validation of incoming JSON data by detecting whether a field was intentionally updated or left unmodified.

How does the update workout handler validate and apply updates from a client request?
The handler first decodes the request into a pointer-based struct, then checks each field for non-nil values. If a field is not nil, it updates the corresponding field in the existing workout object before calling the UpdateWorkout method in the data store.

What is the purpose of using json tags in the UpdateWorkoutRequest struct?
JSON tags define how struct fields are mapped during JSON decoding, allowing the fields to match the expected JSON keys from the client request. This ensures that JSON data is correctly parsed into the struct fields during the update process.

L34
What SQL operation is used to remove a record from the workouts table?
DELETE FROM workouts WHERE id = ?

What HTTP method is used to delete a resource in RESTful design?
DELETE

What HTTP status code is typically returned for a successful deletion with no content to send back?
HTTP 204 No Content

When deleting a workout, what additional check is performed to verify the deletion was successful?
Check the number of rows affected, return sql.ErrNoRows if 0 rows were affected

What are the steps to implement a delete operation in a database-driven application?
Update database layer interface, 2. Create delete method in database store, 3. Create handler function, 4. Add route for delete operation

L35
What is the purpose of the Envelope type in the writeJSON function?
The Envelope is a map with a string key and an interface{} value, allowing flexible JSON serialization by enabling different types of data to be passed, such as error messages or success data like workout structs

How does json.MarshalIndent() differ from standard JSON marshaling?
It allows for indentation of JSON fields by specifying a prefix and indent, making the JSON output more readable compared to compact JSON

What does the chi.URLParam() function do in the readIDParam function?
It extracts a URL parameter from an HTTP request, specifically retrieving the 'ID' parameter as a string from the request

What conversion is performed on the ID parameter in the readIDParam function?
The ID parameter is converted from a string to an int64 using strconv.ParseInt() with base 10, allowing numeric processing of the URL parameter

What are the two primary functions being created in the utils package?
The two functions are writeJSON (for standardized JSON response writing) and readIDParam (for extracting and converting ID parameters from HTTP requests)

L36

How can you improve error handling when writing server responses using a logger?
Use a logger to write detailed error messages, utilize the utils.writeJSON function with specific HTTP status codes, and create an Envelope with an error key to send structured error responses to the client

What is the purpose of the utils.Envelope function in JSON responses?
The utils.Envelope function provides a flexible way to structure JSON responses by allowing dynamic key-value pairs, making it easier to return different types of data with a consistent response format

What are the recommended best practices for handling errors in HTTP handlers?
Log detailed error messages for internal debugging, use appropriate HTTP status codes (like StatusBadRequest or StatusInternalServerError), and provide generic error messages to clients that don't expose sensitive internal details

What HTTP status codes are typically used for different error scenarios?
StatusBadRequest (400) for client-side errors like invalid input, StatusInternalServerError (500) for server-side errors like database failures, and StatusOK (200) for successful operations

How can you simplify JSON response handling in Go HTTP handlers?
Create a utility function like utils.writeJSON that accepts an http.ResponseWriter, status code, and an Envelope, which abstracts the process of writing JSON responses and error handling